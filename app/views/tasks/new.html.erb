<h2 class="title">
  New task
  <%= link_to 'Cancel', tasks_path, class: 'btn btn-primary right' %>
</h2>

<%= form_for @task do |f| %>
  <%= f.label :name, 'Name:' %>
  <%= f.text_field :name %><br />
  <%= f.submit "Create" %>
<% end %>
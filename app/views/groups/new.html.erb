<h2 class="title">
  New group
  <%= link_to 'Cancel', groups_path, class: 'btn btn-primary right' %>
</h2>

<%= form_for @group do |f| %>
  <%= f.label :name, 'Name:' %>
  <%= f.text_field :name, autofocus: true %><br />
  <%= f.submit "Create" %>
<% end %>

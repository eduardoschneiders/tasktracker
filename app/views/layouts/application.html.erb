<!DOCTYPE html>
<html>
  <head>
    <title>Task tracker</title>
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "https://www.google.com/jsapi"%>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, height=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0" />
  </head>
  <body>
    <%= render partial: 'shared/menu' %>
    <div class="container">
      <div id="notice" class="alert alert-success text-success" style="<%= 'display: none' unless flash[:notice]%>">
        <%= flash[:notice] %>
      </div>
      <%= yield %>

      <hr>

      <footer>
        <p>© Task Tracker <%= Date.today.strftime('%Y') %></p>
      </footer>
    </div>
    <%= yield :footer_javascripts%>
  </body>
</html>
